import { NavLink } from "react-router-dom"

const LoginPage = () => {
    return (
        <div>login
            <NavLink to="/register"> register</NavLink>
        </div>
    )
}

export default LoginPage